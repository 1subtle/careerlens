# CareerLens frontend. Copy to .env.local in this directory.
# Set proxy targets before production builds.
BACKEND_ORIGIN=http://127.0.0.1:8000
NEXT_PUBLIC_API_URL=/
# Match backend REQUEST_TIMEOUT_SECONDS * 1000.
NEXT_PUBLIC_REQUEST_TIMEOUT_MS=240000
