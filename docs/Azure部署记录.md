# Azure 部署记录

部署日期：2026-09-09。

## 配置

- 网站域名：[careerlens.cc](https://careerlens.cc/)。`www` 与旧 Azure 域名自动跳转到此地址。
- Azure for Students 订阅，支出上限保持开启；未升级按量付费。
- 资源组：`careerlens`，服务器 `careerlens-web`，日本东部区域。
- Ubuntu 24.04，`Standard_B2ats_v2`，2 vCPU、1 GB 内存、8 GB swap、64 GB 持久 SSD。
- Docker Compose 网站模式，Caddy 管理 HTTPS。对公网开放 80/443；22 仅允许部署来源 IP，应用 3000 与后端 8000 不公开。
- 邮件采用 Azure Communication Services Email 与已验证的 `careerlens.cc` 自定义域名，发件地址为 `CareerLens <DoNotReply@careerlens.cc>`。SMTP STARTTLS 发送测试已通过，自定义域名默认限额为全订阅 100 封/小时、30 封/分钟。
- 网站使用运营者已配置的 `deepseek-v4-flash`。模型及 SMTP 密钥存于服务器的私密 `.env.hosted`，权限 600，不进入源码镜像或 Git。
- 网站使用独立 `hosted-data` 卷，本机已有简历没有上传。
- 已验证邮箱一次性赠送 20 积分，成功模型生成扣 1 积分；完整润色包含生成及核验，可能消耗多积分。模型调用失败或取消退回该次预扣，已成功步骤仍计费。

## 构建修复

- 补全依赖锁文件，修复干净 Linux 环境下 npm ci 失败。
- 构建期准备固定版本并校验的 E5 模型；首次启动离线补齐到数据卷。
- 构建期实际启动 Chromium 检查 PDF 渲染依赖。
- 启动与健康检查同时覆盖 Python 后端和 Next.js；任何子进程失败均会清理其余进程。
- 构建 Node 内存上限 2048 MB，运行上限 512 MB，配合小规格服务器的 swap。

10 项启动模拟自检已在服务器 Bash 5 下通过。2026-09-09 已完成首个正式镜像构建，应用健康检查通过；Caddy 成功取得 Let's Encrypt 证书，HTTP 请求自动跳转 HTTPS。

积分版本的后端注册、迁移、并发扣减、失败退款、额度耗尽及已有业务回归共 246 项通过；前端相关测试 35 项、TypeScript 与改动文件 ESLint 通过。旧账号迁移使用唯一用户标识保证只赠送一次；重新登录和重启不恢复已用积分。网站状态检查不调用付费模型。

通过新网址的公网 `/api/v1/auth/email/start` 向运营者 Azure 账户邮箱提交验证码，接口返回 200，用时 5.93 秒，SMTP 服务接受发送；收件箱实际到达尚未人工确认。自定义域名发信已生效，原托管域名作为回退关联保留，默认额度依据 [Azure 官方邮件限额](https://learn.microsoft.com/en-us/azure/communication-services/concepts/service-limits#email)。

## 线上验收结果

积分版已正式发布，应用与 Caddy 正常运行，公网健康接口返回 200，未登录访问积分接口返回 401。镜像标识为 `sha256:b697e998afb80aea81933f7914589ec74b550cb4dc89e2b6ae7604715ea7c721`。

新域名切换后运行 [线上验收脚本](../scripts/qa_hosted.py)，69 条检查及事件记录整体通过，用时 53.82 秒：

- 两个合成邮箱通过真实验证接口分别创建账号，各获 20 积分，认证 Cookie 包含 Secure 与 HttpOnly。
- 真实调用 `deepseek-v4-flash` 完成简历段落润色与事实核验，来源引用检查通过；操作账号从 20 扣至 18，另一账号仍为 20。
- 采纳后创建独立定向简历，原简历保留，重复采纳不重复创建。
- 中文 PDF 与原生可编辑 Word 导出成功，均包含润色后的正文；PDF 为 1 页，本次首次导出用时 39.89 秒。
- 其他账号无法访问诊断、采纳建议或导出这些简历；规则操作、采纳和导出不扣积分。
- 再次邮箱登录保持 18 积分；验收结束仅清理这次创建的两个测试账号、积分账户和工作区。

新域名、邮件及完整使用流程结果见 [careerlens.cc 验收 JSON](evidence/careerlens-cc-2026-09-09.json)；首次上线结果保留在 [Azure 初次验收 JSON](evidence/azure-hosted-2026-09-09.json)。积分升级前已停止应用并备份完整数据卷，备份保存在服务器 `/home/careerlens/backups/before-credits-20260909T105104Z.tar.gz`，权限 600；旧镜像保留为 `careerlens:before-credits`。

## 管理

服务器项目目录为 `/home/careerlens/app`。常用命令：

```bash
cd /home/careerlens/app
sudo docker compose --env-file .env.hosted -f compose.hosted.yaml ps
sudo docker compose --env-file .env.hosted -f compose.hosted.yaml logs --tail=50
```

更新、备份沿用[网站部署指南](网站部署指南.md)。不要删除数据卷；持久存储不代替独立备份。

SMTP 凭据有效期至 2027-09-09，学生赠金额度到期时间为 2027-09-09。需要在到期前处理续期和凭据更换。初期小规模使用可利用免费机型时数及学生赠金；IP、邮件、超出免费额度的资源仍会计量，AI 供应商费用独立计算。

## Cloudflare 独立域名部署（已完成）

2026-09-09 用户在 Cloudflare 购买 `careerlens.cc` 并导入 DNS，网站已切换完成。域名使用 `joaquin.ns.cloudflare.com` 和 `blair.ns.cloudflare.com`；根域名 A 与 `www` CNAME 均通过公共 DNS over HTTPS 核对。

`careerlens.cc` 提供网站，`www.careerlens.cc` 和旧 Azure 域名以 301 跳转到根域名并保留路径与查询；HTTP 以 308 跳转 HTTPS。服务器及本机 `.env.hosted` 已同步为新域名和新发件地址，应用与 Caddy 容器均已重建，继续使用原镜像、认证密钥与 `careerlens-hosted_hosted-data` 数据卷。用户在新域名重新登录后继续使用原账号、简历和积分。

正式 [Caddy 配置](../deploy/Caddyfile) 已通过线上校验并载入，根域名和 `www` 的 HTTPS 证书均通过公网客户端正常校验。最终源文件 SHA-256 为 `f2ff9ceb4615b46d616198522c6ea9e2e0c2a06458be297492ef21cbe94ae8bc`，本机与服务器一致。公网 7 项只读验收全部通过。

切换前的 `.env.hosted` 与 Caddy 配置备份在 `/home/careerlens/backups/before-careerlens-cc-20260909`，目录权限 700、文件权限 600。本次切换成功，未触发回退。

已导入的 [DNS 配置文件](../deploy/careerlens.cc-dns.txt) 共 7 条记录：2 条网站解析、Azure 返回的 4 条邮件验证记录，以及初始 DMARC 观察策略。A 与 CNAME 显式设置 `cf-proxied:false`，保持 DNS only。网站记录为：

| 类型 | 名称 | 内容 | 代理状态 |
|---|---|---|---|
| A | `@` | `74.176.210.32` | DNS only |
| CNAME | `www` | `careerlens.cc` | DNS only |

网站解析使用 DNS only，以保留当前验证码按真实来源 IP 限流的行为；DKIM CNAME 同样保持 DNS only。

`careerlens-email` 下的 `careerlens.cc` 自定义发件域名状态为 `Succeeded`、`CustomerManaged`，互动追踪为 `Disabled`。Domain、SPF、DKIM、DKIM2 全部 `Verified`，已追加关联 `careerlens-comm-9cffa58d`，原托管域名关联完整保留，共 2 个关联域名。实际发件地址 `DoNotReply@careerlens.cc` 已启用，继续使用原 SMTP 凭据。DMARC 的 `p=none` 记录已发布，Azure 未另行标记其验证完成。

发信域名上线与网站域名切换分别验证。自定义域名默认额度为全订阅 100 封/小时、30 封/分钟，见 [Azure 邮件限额](https://learn.microsoft.com/en-us/azure/communication-services/concepts/service-limits#email)。自定义 `login@` 等发件用户名另受[提高默认发送限额的条件](https://learn.microsoft.com/en-us/azure/communication-services/quickstarts/email/add-multiple-senders)限制。

## 账号隔离与安全修复更新

2026-09-09 22:24（北京时间）发布安全检查版本，当前镜像为 `sha256:4d4b5e3a386ad62c237d8dc8c145cb7b451695c0ea3f31125e009bcd394994c8`。修复岗位及投递的关联简历归属校验、删除岗位后的预览残留、供应商异常日志敏感内容；同步发布费用承担文案删除及安全依赖更新。域名、发信配置和持久数据卷沿用现有配置。

本地后端 1246 项、前端 576 项测试通过；生产构建通过。服务器严格安装曾发现跨平台锁文件缺项，补全 5 条依赖记录后 `npm ci` 与 Linux 构建通过，原有依赖版本未因锁文件修复改变。

发布后双账号验收整体通过，用时 53.23 秒：物理数据库分别存储，跨账号读写/删除/导出被拒绝；真实润色及事实核验扣 2 积分，另一账号余额不变；中文 PDF 和原生 Word 导出成功，再次登录不重赠积分。两份测试工作区和测试账号均已清理，最终数据库完整性和 13 项公网检查通过。线上构建产物中指定费用承担文案匹配数为 0。

停写备份为 `/home/careerlens/backups/before-audit-20260909T142414Z.tar.gz`（权限 600），旧镜像保留为 `careerlens:before-audit-20260909T142414Z`。未触发回退。

完整结论、测试范围与仍需处理的预算、备份、容量和积分流水限制见 [账号隔离与安全检查](账号隔离与安全检查_2026-09-09.md)，脱敏结果见 [安全检查 JSON](evidence/account-security-2026-09-09.json)。

## AI 诊断错误修复更新

2026-09-09 23:33（北京时间）开始发布诊断修复，应用及 Caddy 健康检查通过。当前镜像为 `sha256:fdebb355041a87573c6159316f41b6dbda8b0c17d48109af4a45e2c08f8bdf21`。修复合法建议的角色及表达误判、校验问题反馈不完整、JSON 解析失败缺少重试，并区分错误提示；岗位匹配、批量比较和方向推荐在整次操作失败时退回本次操作的积分。严格原文引用及独立事实复核继续执行。

最终后端 1287 项回归通过，发布镜像内 65 个后端源文件与测试版本的哈希一致。本次仅替换四个后端文件，沿用此前已验收的前端与依赖镜像；完整项目源码同步至服务器，后续常规构建使用相同代码。

发布后双账号验收 95 项检查及事件整体通过，用时 88.50 秒。真实 AI 岗位匹配 18.23 秒、方向推荐 8.22 秒，均返回 200 并通过事实复核；匹配结果正确保存、另一账号访问返回 404。真实改写、中文 PDF、原生 Word 和积分隔离检查通过，两个测试账号及工作区已清理。公网首页 200，未登录访问私有工作区 401。

停写备份为 `/home/careerlens/backups/before-diagnosis-20260909T153322Z.tar.gz`（权限 600），原镜像保留为 `careerlens:before-diagnosis-20260909T153322Z`，未触发回退。完整说明见 [AI 诊断失败排查与修复](AI诊断失败排查与修复_2026-09-09.md)。

## 规则、历史与账务升级（最新）

2026-09-10 12:48（北京时间）开始发布，应用健康后恢复公网访问。新镜像为 `sha256:21bc40d1970b6fb50802990012acf2a1615b4d0e4168e45dfbc5e62eb937fab4`，74 个后端文件与本地验证源码一致，前端账务功能已包含在正式构建中。删除评判性 AI 生成门槛，上线 JD 乐观版本控制、方向与统计历史、认证与业务库的编号迁移、逐次积分流水、跨库原子结算和微信订单接口。

发布先停止入口和应用，再冷备数据卷。认证库、操作员业务库及两个用户业务库均升级至版本 2，旧数据记录数、2 个存量账户和 39 积分余额全部保留。恢复入口前完成一致性校验，没有回退。冷备位于 `/home/careerlens/backups/before-billing-20260910T044852Z.tar.gz`（权限 600），旧镜像为 `careerlens:before-billing-20260910T044852Z`。

冻结源码的后端 1298 项、前端 587 项测试及类型、Lint 检查已通过；续作独立复核另通过后端 149 项、前端 16 项定向检查。线上双账号验收用时 62.12 秒，118 项检查及事件通过：实际润色、岗位匹配、方向推荐、逐次积分流水、JD 409 冲突、历史快照保留和跨账号访问拒绝，以及中文 PDF、原生 Word 导出。测试账号及目录已清理，最终余额仍为 39，预留为 0，账务差异与完整性错误均为 0。10 项公网状态检查全部通过。

已实测桌面首页、桌面及手机登录页的浏览器呈现；未取得已登录账户页和历史页面的视觉证据。运营者目前没有营业执照，真实微信收款保持关闭，暂定价格仅保存为待启用配置。参见[规则调整与账务升级](规则调整与账务升级_2026-09-10.md)、[积分定价与微信收款说明](积分定价与微信收款说明_2026-09-10.md)和[脱敏验收记录](evidence/billing-upgrade-2026-09-10.json)。


## 2026-09-10 个人中心升级

已发布独立个人中心，包含钱包管理、充值账单、消费日志、个人设置、账号安全和数据管理。网页中英文及个人时区按账号保存，新增会话撤销与当前用户数据导出。详细功能见[个人中心升级](个人中心升级_2026-09-10.md)，匿名验证结果见[本次证据](evidence/account-center-2026-09-10.json)。

镜像 `sha256:deef64546ebbf40fb1893b637aeec4ebc120d6eb9bb02e37a859fd68b4979d9b`；认证库升级为第 3 版、业务库保持第 2 版。切换前冷备份为 `/home/careerlens/backups/before-account-20260910T063053Z.tar.gz`，旧镜像标签 `careerlens:before-account-20260910T063053Z`。恢复入口前逐表核对原有记录与余额一致。

前端 617 项、后端 1328 项测试通过；线上双账号验收记录 159 项响应与断言全部通过，另有 12 项公网检查通过。合成测试账号已清理，最终只读审计保持 2 个真实账号、合计 39 积分，业务库、偏好与账目一致。真实微信收款仍关闭。

Browser 无可用连接、Computer Use 读取 Chrome 未返回，本轮未完成桌面或手机截图验收，不能将该项记为通过。

## 2026-09-10 个人中心界面整合更新

用户重新完成 Azure 登录，已恢复部署连接，SSH 入站来源限定为 `120.245.90.0/32`。切换前已确认运行镜像与个人中心版本基线一致，只读数据库检查通过，保留 2 个账户、2 个用户工作区。

前端候选镜像 `careerlens:ui-20260910` 已构建并通过镜像检查，复用原后端、Node、启动脚本和运行配置。前端路由、后端代理及预览模拟标记检查通过。本次界面调整不涉及数据库结构变更，详细内容见[个人中心界面整合](个人中心界面整合_2026-09-10.md)。

服务器实测总内存为 897 MiB；构建期间公网响应曾短暂延迟，构建完成后、正式切换前首页恢复 HTTP 200。后续应使用独立构建环境生成镜像，再传至服务器发布。

2026-09-10 约 16:55（北京时间）完成正式激活，激活脚本正常退出，应用健康检查通过，Caddy 已启动。运行镜像为 `sha256:7b7816b11ee8cd4caab2b3d681d877c9f5ec1372a2f9141b714ee5af737f5c02`；旧镜像保留为 `careerlens:before-ui-20260910T085318Z`。

发布后 20 项公网只读检查全部通过。数据库审计快照与切换前完全一致，完整性检查通过，保持 2 个账户、2 个用户工作区，积分余额合计 39；本次验收未写入数据。完整结果见[脱敏发布验收记录](evidence/unified-ui-release-2026-09-10.json)。

同一发布归档经全部清单哈希验证后，已同步 14 个发生变化的前端源码文件至正式项目目录；再次执行同步确认 0 个文件变化，最终应用状态健康。被覆盖源码备份为 `/home/careerlens/backups/before-ui-source-20260910T085517053821Z.tar.gz`（权限 600）。正式网站的浏览器复验因工具超时及内核重置未完成；本次视觉截图来自本地合成账户，不作为线上登录态验收证据。

## 2026-09-10 界面空间与诊断质量改进

约 17:55（北京时间）发布：右侧主区域统一滚动、账户表格字号与行间距增大、目标岗位主体内容直接展开、5 个国内平台官网搜索入口，以及诊断事实表述与无关引文结构修复。详细内容与截图见[本轮改进](界面空间与诊断质量改进_2026-09-10.md)。

本轮改用本机专用 Linux amd64 环境构建，完成后停止该虚拟机；线上服务器仅封装产物。运行镜像 `sha256:ec9fe43e05ceaf734e9db6491867d1fdb8a9c18deb6c26a833d33ada1c5667ae`，回退镜像 `careerlens:before-ux-20260910T095320Z`。镜像校验确认除 `career_diagnosis.py` 外其余后端、依赖、Node 和运行配置未变。

前端 630 项、后端 1331 项测试验证通过；真实模型以合成简历完成 8 次调用，三家站内招聘来源各成功返回 10 条完整 JD。上线健康与 20 项公网检查通过，前后数据库审计完全一致（2 个账户、2 个工作区、合计 39 积分）。[脱敏发布证据](evidence/ux-release-2026-09-10.json)。

正式源码同步 10 个前端文件及 1 个诊断文件，再次同步无变化。覆盖前备份 `/home/careerlens/backups/before-ux-20260910-source-20260910T095509983700Z.tar.gz`（权限 600）。数据库结构、生产账户及账目未因本次验收改动。

## 2026-09-10 GitHub 提示词接入

主润色入口已接入 Resume-Matcher 的原始策略及适配后的中文核心模板。以 UX 发布镜像 `ec9fe43e05ce` 为基线做两文件后端覆盖，活动镜像为 `sha256:2aac6b2287b92173538367e8945d64d48571ee888bdb46351a4b50483519ea39`；回滚 tag 为 `careerlens:before-rewrite-20260910T103332Z`。前端及其他运行文件指纹一致，未安装依赖。

92 项相关回归测试通过；旧版/适配版/最终版对照及部署后新案例共 13 次真实模型请求，均使用合成材料和临时账号。20 项公开页面/接口检查通过，部署前后只读存储审计摘要一致。详见 [GitHub 提示词接入与复测](GitHub提示词接入与复测_2026-09-10.md) 和 [发布证据](evidence/rewrite-release-2026-09-10.json)。
