# CareerLens 缺陷与修复记录

更新日期：2026-09-09。本记录汇总历史缺陷、已落实的修复及仍需处理的工程问题。2026-09-09 本次工作只补齐文档并执行专项回归，没有修改业务源码。

## 1. 状态与优先级

P0 表示影响数据、权限或核心闭环；P1 表示影响交付复现或重要功能；P2 表示可继续使用但需改进。状态分为“已修复并有回归证据”“待处理”“待目标环境验证”。历史修复日期按原记录填写，后续复验时间单独列出。

## 2. 已有修复及回归

| 编号 / 级别 | 触发条件与原表现 | 原因与修复 | 修复证据与本轮复验 | 状态 |
| --- | --- | --- | --- | --- |
| BUG-01 / P0 | 只编辑简历原文，保存返回 200，重载却仍是旧原文 | 更新路径原先未同步 source_text；现在同步原文、结构化字段及缓存，并将源文本纳入冲突检查 | 9/7 F1；本轮 `test_source_only_edit_roundtrip_and_conflict` 通过；TC-03 | 已修复并复验 |
| BUG-02 / P1 | 中文 DOCX 已提取正文，但姓名、教育和项目分类错误；导入未遵循 AI 开关 | 文件接口透传 AI 开关；规则解析扩展中文标题、跨行教育、日期；AI 失败保留原文及规则草稿 | 9/7 F2；本轮 `test_file_ai_switch_and_failure_preserves_extracted_text` 及规则解析通过；TC-01—02 | 已修复并复验指定样例 |
| BUG-03 / P1 | 任务表述与关键词不同，相关经历全部被标成无证据 | 增加常见别名和本地语义候选；由用户核对是否构成证据 | 9/7 F3；本轮 `test_semantic_candidates_do_not_assign_scores_and_fallback` 通过；TC-10 | 候选机制已修复；真实匹配质量待评估 |
| BUG-04 / P0 | 原文仅为“参与问卷”，旧检查允许“主导上线、提升留存”等扩写 | 扩展数字、技能、角色、结果与用途检查，增加逐句引文核验；失败不存草稿 | 9/7 F4；本轮无来源数字、角色和用途反例通过；TC-11 | 已修复并复验 |
| BUG-05 / P1 | 草稿只改空格或标点，仍被描述为明显优化 | 增加改善程度检查与一次受限重试，清楚区分 improved/unchanged | 9/8 AI 摘要；本轮 `test_rewrite_structure_review_and_one_unchanged_retry` 通过；TC-12 | 已修复并复验 |
| BUG-06 / P1 | 调整版式后，另一窗口可用旧内容覆盖新排版 | 用包含版式的 revision 检查保存冲突；事实哈希保持独立 | 9/8 排版记录；本轮 `test_layout_revision_blocks_old_window_without_invalidating_evidence` 通过；TC-03、19 | 已修复并复验 |
| BUG-07 / P1 | 关闭终端后残留服务仍占端口，PDF 导出失败 | 服务日志写入文件，启动脚本清理进程组并处理 SIGHUP | 9/7 首版记录含 SIGINT/SIGTERM/SIGHUP 及 PDF 复验 | 历史已修复；本轮未重启用户服务 |
| BUG-08 / P2 | 窄屏图表网格撑宽页面 | 网格允许子项收缩，表格约束在容器内 | 历史 390 px 与 524 px 实际宽度记录 | 历史已修复；本轮未做浏览器测量 |
| BUG-09 / P1 | 临时 Webpack 开发副本出现 jsdom 样式路径错误，影响打印 | 交付版本使用实际 Turbopack 生产 standalone 构建；检查打印追踪文件与依赖 | 9/8—9 网站模式运行记录中 PDF/Word 成功 | 生产路径历史通过；本轮未重新构建 |

主要历史证据：[自查报告](项目自查报告_2026-09-07.md)、[增强修复](增强功能与缺陷修复_2026-09-07.md)、[AI 摘要](evidence/ai-diagnosis-2026-09-08.json)、[排版导出](简历照片与排版导出.md)、[网站模式验证](网站模式验证记录.md)。本轮回归编号为 R-20260909-B/F，详细数量见[测试报告](测试报告.md)。

## 3. 当前工程问题

| 编号 / 级别 | 已知情况与影响 | 处理要求 | 关闭标准 |
| --- | --- | --- | --- |
| OPEN-01 / P1 | `requirements.txt` 未列出所有当前后端运行依赖，单独 pip 安装不能代表可复现安装 | 当前采用 `pyproject.toml` + `uv.lock` 冻结安装；后续同步或明确归档旧清单 | 干净环境仅按正式安装说明即可启动和测试；依赖说明不再相互矛盾 |
| OPEN-02 / P1 | 根目录 `data/schema.sql` 是较早业务表快照，缺当前 `template_settings` 等字段，且不含独立认证数据库的 4 张表 | 初始化以应用 ORM 和 hosted 启动逻辑为准；业务 DDL 用 `career_data schema` 导出至新文件核对；认证 DDL 按认证模块另行导出 | 两类数据库的当前结构、数据字典和交付 SQL 一致，执行与迁移验证通过 |
| OPEN-03 / P2 | 保存实时 JD 时记录入库时间，未完整保留原查询的获取时间 | 统一来源时间字段及界面含义 | 查询获取时间、来源发布日期和入库时间分别可追溯 |
| OPEN-04 / P2 | 部分市场界面未显式给出薪资有效数/缺失数；Jobicy 薪资未纳入统一统计口径 | 明示分母与有效样本；不能直接合并异币种、异周期薪资 | 同一筛选下样本、有效数和缺失数可复算 |
| OPEN-05 / P1 | 删除 JD 后，上游部分无外键扩展日志的清理范围尚需补齐 | 按接口与数据库设计确认所有相关记录，增加针对性删除回归 | 删除后无应清理的悬空关联，独立简历保留 |
| OPEN-06 / P2 | 证据人工复核后沿用的 AI 判断需要更明确标示其生成时点；首次远程传输告知仍可完善 | 对齐当前快照、复核记录与旧 AI 结果；明确材料发送范围 | 用户能识别哪些判断已更新，哪些尚未重新生成 |

上述清单记录源码与既有需求所反映的问题；本次未将其标为已修复。部署说明给出的冻结安装和应用初始化是当前可用路径。

## 4. 验收缺口

| 编号 | 缺口 | 计划验证 | 当前状态 |
| --- | --- | --- | --- |
| GAP-01 | 缺完整真实样本与人工质量结果 | 3 位学生、5 份 JD、标注和改写忠实性复核 | 待材料与执行 |
| GAP-02 | 缺性能分布与 768 px / 完整键盘验收 | TC-25—26 | 待执行 |
| GAP-03 | 缺干净环境安装和备份恢复记录 | TC-27 | 待执行 |
| GAP-04 | 缺目标服务器 Docker、HTTPS、真实邮件验证 | TC-28 | 待目标环境验证 |

待验收项不等同于已发现运行失败。2026-09-10 至 09-12 由实际执行者追加日期、修复与结果，已有记录保留。
